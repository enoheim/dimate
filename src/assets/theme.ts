import { createMuiTheme } from '@material-ui/core/styles'

export const theme = createMuiTheme({
  palette: {
    primary: {
      light: '#ffedab',
      main: '#ffedab',
      dark: '#ffedab',
    },
    secondary: {
      light: '#24272d',
      main: '#24272d',
      dark: '#24272d',
    },
  },
})
